const fetchName = () => {
    console.log("fetching name")
    setTimeout(() => {
     const data = "after 2 seconds"
    }, 2000)
};

const displayName = (data) => {
    console.log(data)
};

fetchName(displayName);


printName(() => { "lucas.w"
printAge(() => { "26"
printLocation(() => { "sweden"
printHobbies(() => { "gaming, coding"

});
});
});
});